$.each($(".component-sample"), (function() {
        var t = $(this).html()
            , e = {
            "<": "&lt;",
            ">": "&gt;",
            "  ": ""
        };
        t = t.replace(/<|>|  /gi, (function(t) {
                return e[t]
            }
        )),
            $(this).append('<pre class="code-sample copy-this card bg-light"><code>' + t + '</code></pre><p><button class="btn copy-btn" data-toggle="tooltip" data-placement="top" title="Copy Code To Clipboard">Copy Code</button></p>')
    }
)),
    $(".copy-btn").on("click", (function() {
            var t = $(this).parent().prev(".copy-this")
                , e = String(t[0].textContent)
                , a = document.createElement("textarea");
            a.value = e,
                document.body.appendChild(a),
                a.select(),
                document.execCommand("copy"),
                document.body.removeChild(a),
                $(this).html("Code Copied")
        }
    )),
    $.each($(".accordion"), (function(t) {
            $(this).attr("id", "accordion_" + parseInt(t + 1))
        }
    )),
    $.each($(".accordion > .card"), (function(t, e) {
            var a = t + 1;
            $(e).children(".card-header").attr("id", "heading_acc_" + a),
                $(e).find(".card-header > .card-title").wrapInner('<button  class="btn btn-link" type="button" data-toggle="collapse" aria-expanded="false"></button>'),
                $(e).find(".card-header > .card-title > button").attr({
                    "data-target": "#collapse_acc_" + a,
                    "aria-controls": "collapse_acc_" + a
                }),
                $(e).children(".collapse").attr({
                    id: "collapse_acc_" + a,
                    "aria-labelledby": "heading_acc_" + a
                })
        }
    )),
    $(".expandall").on("click", (function() {
            var t = $(this).parent().next(".accordion");
            $(this).next(".accordion").length > 0 && (t = $(this).next(".accordion"));
            var e = "#" + t.attr("id");
            $(e).hasClass("show-all") ? ($(e + " .collapse.show").collapse("hide"),
                $(e).removeClass("show-all"),
                $(this).attr("aria-expanded", "false"),
                $(this).text("Open All Panels")) : ($(e + ' .collapse:not(".show")').collapse("show"),
                $(e).addClass("show-all"),
                $(this).attr("aria-expanded", "true"),
                $(this).text("Close All Panels"))
        }
    )),
    $(".card-header").find("button").click((function() {
            for (var t = !1, e = 0; e < $(".card-header").find("button").length; e++)
                if (this === $(".card-header").find("button")[e]) {
                    if ("false" === $($(".card-header").find("button")[e]).attr("aria-expanded")) {
                        t = !0;
                        break
                    }
                } else if ("true" === $($(".card-header").find("button")[e]).attr("aria-expanded")) {
                    t = !0;
                    break
                }
            var a = $(this).closest(".accordion")[0]
                , n = $(this).closest(".row").find(".expandall")[0];
            !0 === t ? ($(a.id + ' .collapse:not(".show")').collapse("show"),
                $(a).addClass("show-all"),
                $(n).text("Close All Panels")) : ($(a.id + ".collapse .show").collapse("hide"),
                $(a).removeClass("show-all"),
                $(n).text("Open All Panels"))
        }
    ));
for (var bgImgWrapper = document.getElementsByClassName("bg-img-wrapper"), bgImgIndex = 0; bgImgIndex < bgImgWrapper.length; bgImgIndex++) {
    var bgImgWrapperId = "bg-img-wrapper-" + parseInt(bgImgIndex);
    document.querySelectorAll(".bg-img-wrapper")[bgImgIndex].setAttribute("id", bgImgWrapperId);
    var bgImg = document.getElementById(bgImgWrapperId).getElementsByTagName("img")[0].src
}
$.each($(".card-reveal"), (function(t, e) {
        var a = t + 1;
        $(e).find(".card-body > .card-text > .btn-reveal").attr({
            "data-target": "#c2r" + a,
            id: "c2s" + a
        }),
            $(e).find(".card-body > .card-text > .collapse").attr({
                id: "c2r" + a,
                "aria-labelledby": "c2s" + a
            })
    }
)),
    $.each($(".overlay-reveal"), (function(t, e) {
            var a = t + 1;
            $(e).find(".btn").attr({
                "data-target": "#collapseExample" + a,
                "aria-controls": "collapseExample" + a
            }),
                $(e).find(".collapse").attr({
                    id: "collapseExample" + a
                })
        }
    )),
    $.each($(".table-reveal tr"), (function(t, e) {
            var a = t + 1;
            $(e).find(".btn-reveal").attr({
                "data-target": "#tc2r" + a,
                id: "tc2s" + a
            }),
                $(e).find(".collapse").attr({
                    id: "tc2r" + a,
                    "aria-labelledby": "tc2s" + a
                })
        }
    )),
    $(".btn-reveal").on("click", (function() {
            var t = $(this);
            setTimeout((function() {
                    t.attr("disabled", !0)
                }
            ), 500)
        }
    )),
    $(".flip-card-content").on("click", (function() {
            $(this).toggleClass("apply-flip")
        }
    )),
    $(".flip-card-content").keydown((function(t) {
            13 === (t.keyCode ? t.keyCode : t.which) && $(t.target).trigger("click")
        }
    )),
    $("a[target='_blank']").addClass("new-window"),
    $("a[download]").addClass("download"),
    $(".download, .new-window").html((function() {
            if ((t = $(this).text().split(" ")).length > 1) {
                var t, e = (t = $(this).text().split(" ")).pop();
                return t.join(" ") + (t.length > 0 ? ' <span class="lastWord">' + e + "</span>" : e)
            }
            $(this).wrapInner(' <span class="lastWord"></span>')
        }
    )),
    $((function() {
            $(".download, .new-window").after("&nbsp;")
        }
    )),
    $((function() {
            $('[data-toggle="tooltip"]').tooltip()
        }
    ));
for (var links = document.querySelectorAll("a"), downloadIndex = 0, newWindowIndex = 0, linkIndex = 0; linkIndex < links.length; linkIndex++) {
    var srTxtWrapper = document.createElement("span");
    if (srTxtWrapper.classList.add("sr-only"),
        links[linkIndex].classList.contains("download")) {
        links[linkIndex].setAttribute("id", "download-file-" + downloadIndex),
            links[linkIndex].setAttribute("title", "download file"),
            links[linkIndex].setAttribute("data-toggle", "tooltip");
        var srTxt = document.createTextNode("(this link downloads a file)");
        srTxtWrapper.appendChild(srTxt),
            links[linkIndex].append(srTxtWrapper),
            downloadIndex++
    } else if (links[linkIndex].classList.contains("new-window")) {
        links[linkIndex].setAttribute("id", "new-window" + newWindowIndex),
            links[linkIndex].setAttribute("data-original-title", "opens in new window/tab"),
            links[linkIndex].setAttribute("data-toggle", "tooltip"),
            links[linkIndex].setAttribute("rel", "noopener");
        srTxt = document.createTextNode("(this link opens in a new window/tab)");
        srTxtWrapper.appendChild(srTxt),
            links[linkIndex].append(srTxtWrapper),
            newWindowIndex++
    }
}
$.each($(".modal-set"), (function(t, e) {
        var a = t + 1;
        $(e).find(".btn").attr({
            "data-target": "#exampleModal" + a
        }),
            $(e).find(".modal").attr({
                id: "exampleModal" + a,
                "aria-labelledby": "exampleModalLabel" + a
            }),
            $(e).find(".modal-title").attr({
                id: "exampleModalLabel" + a
            })
    }
)),
window.self !== window.top && $('[data-toggle="modal"]').click((function() {
        var t;
        $(this).hasClass("btn-breakout") ? (console.log("btn-breakout found"),
            t = $(this).parent().offset().top) : t = $(this).offset().top,
            $(".modal").on("show.bs.modal", (function() {
                    $(this).css("padding-top", t)
                }
            ))
    }
)),
    $(".modal").on("hide.bs.modal", (function() {
            var t = $(this).html();
            $(this).html(t)
        }
    )),
    $((function() {
            $('[data-toggle="popover-top"]').popover({
                html: !0
            }),
                $('[data-toggle="popover-bottom"]').popover({
                    html: !0
                }),
                $('[data-toggle="popover-left"]').popover({
                    html: !0
                }),
                $('[data-toggle="popover-right"]').popover({
                    html: !0
                })
        }
    )),
    $.each($(".quick-assess-mc"), (function(t, e) {
            var a = t + 1;
            $(e).find(".btn-quick-assess").attr({
                "data-target": "#MC-QuizFeedback" + a,
                "aria-controls": "MC-QuizFeedback" + a,
                id: "MC-QuizButton" + a
            }),
                $(e).find(".collapse").attr({
                    id: "MC-QuizFeedback" + a
                });
            var n = "#MC-QuizButton" + a;
            $(e).find("input").on("click", (function() {
                    $(n).removeAttr("disabled")
                }
            )),
                $(n).click((function() {
                        $(e).find(".ans-symbol").removeClass("invisible"),
                            $(e).find(".collapse").on("shown.bs.collapse", (function() {
                                    $(e).find(".btn-quick-assess, input").prop("disabled", !0)
                                }
                            ))
                    }
                ))
        }
    )),
    $.each($(".quick-assess-mc .form-check"), (function(t, e) {
            var a = t + 1;
            $(e).find("input").attr({
                id: "MC-ans" + a,
                value: "MC-ans" + a
            }),
                $(e).find("label").attr({
                    for: "MC-ans" + a
                })
        }
    )),
    $.each($(".quick-assess-ms"), (function(t, e) {
            var a = t + 1;
            $(e).find(".btn-quick-assess").attr({
                "data-target": "#MS-QuizFeedback" + a,
                "aria-controls": "MS-QuizFeedback" + a,
                id: "MS-QuizButton" + a
            }),
                $(e).find(".collapse").attr({
                    id: "MS-QuizFeedback" + a
                });
            var n = "#MS-QuizButton" + a;
            $(e).find("input").on("click", (function() {
                    $(n).removeAttr("disabled")
                }
            )),
                $(n).click((function() {
                        $(e).find(".ans-symbol").removeClass("invisible"),
                            $(e).find(".collapse").on("shown.bs.collapse", (function() {
                                    $(e).find(".btn-quick-assess, input").prop("disabled", !0)
                                }
                            ))
                    }
                ))
        }
    )),
    $.each($(".quick-assess-ms .form-check"), (function(t, e) {
            var a = t + 1;
            $(e).find("input").attr({
                id: "MS-ans" + a,
                value: "MS-ans" + a
            }),
                $(e).find("label").attr({
                    for: "MS-ans" + a
                })
        }
    )),
    $.each($(".quick-assess-sa"), (function(t, e) {
            var a = t + 1;
            $(e).find("input[type=text]").keyup((function() {
                    var t = !1;
                    $(e).find("input[type=text]").each((function() {
                            "" == $(this).val() && (t = !0)
                        }
                    )),
                        t ? $(e).find(".btn-quick-assess").prop("disabled", !0) : $(e).find(".btn-quick-assess").removeAttr("disabled")
                }
            )),
                $(e).find(".btn-quick-assess").attr({
                    "data-target": "#FIB-QuizFeedback" + a,
                    "aria-controls": "FIB-QuizFeedback" + a,
                    id: "FIB-QuizButton" + a
                }),
                $(e).find(".collapse").attr({
                    id: "FIB-QuizFeedback" + a
                }),
                $("#FIB-QuizButton" + a).click((function() {
                        $(e).find(".collapse").on("shown.bs.collapse", (function() {
                                $(e).find(".btn-quick-assess, input").prop("disabled", !0)
                            }
                        ))
                    }
                ))
        }
    )),
    $.each($(".quick-assess-sa .shortanswer"), (function(t, e) {
            var a = t + 1;
            $(e).find("label").attr({
                for: "FIB-answer" + a
            }),
                $(e).find("input").attr({
                    id: "FIB-answer" + a
                })
        }
    )),
    $.each($(".quick-assess-dd"), (function(t, e) {
            var a = t + 1;
            $(e).find("select").change((function() {
                    var t = !1;
                    $(e).find("select").each((function() {
                            null == $(this).val() && (t = !0)
                        }
                    )),
                        t ? $(e).find(".btn-quick-assess").prop("disabled", !0) : $(e).find(".btn-quick-assess").removeAttr("disabled")
                }
            )),
                $(e).find(".btn-quick-assess").attr({
                    "data-target": "#DD-QuizFeedback" + a,
                    "aria-controls": "DD-QuizFeedback" + a,
                    id: "DD-QuizButton" + a
                }),
                $(e).find(".collapse").attr({
                    id: "DD-QuizFeedback" + a
                }),
                $("#DD-QuizButton" + a).click((function() {
                        $(e).find(".collapse").on("shown.bs.collapse", (function() {
                                $(e).find(".btn-quick-assess, select").prop("disabled", !0)
                            }
                        ))
                    }
                ))
        }
    )),
    $.each($(".quick-assess-dd label"), (function(t) {
            $(this).attr({
                for: "DD-answer" + parseInt(t + 1)
            })
        }
    )),
    $.each($(".quick-assess-dd select"), (function(t) {
            $(this).attr({
                id: "DD-answer" + parseInt(t + 1),
                name: "select" + parseInt(t + 1)
            })
        }
    )),
    $.each($(".list-group"), (function(t) {
            $(this).attr("id", "list-tab_" + parseInt(t + 1))
        }
    )),
    $.each($(".list-group > .list-group-item"), (function(t, e) {
            var a = t + 1;
            $(e).attr({
                id: "list-" + a + "-list",
                href: "#list-" + a,
                "aria-controls": "list-" + a
            })
        }
    )),
    $.each($(".tab-content"), (function(t) {
            $(this).attr("id", "nav-tabContent_" + parseInt(t + 1))
        }
    )),
    $.each($(".tab-content > .tab-pane"), (function(t, e) {
            var a = t + 1;
            $(e).attr({
                id: "list-" + a,
                "aria-labelledby": "list-" + a + "-list"
            })
        }
    )),
    $((function() {
            $('[data-toggle="tooltip"]').tooltip(),
                $(".popovers-link").popover({
                    html: !0,
                    trigger: "focus"
                })
        }
    )),
    $(".btn-reveal1").click((function() {
            $(".collapse").collapse("hide")
        }
    ));
